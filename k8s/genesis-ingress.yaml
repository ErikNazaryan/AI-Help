apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: genesis-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: genesis.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: genesis-app-service
            port:
              number: 80