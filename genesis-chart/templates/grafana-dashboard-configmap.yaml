apiVersion: v1
kind: ConfigMap
metadata:
  name: genesis-dashboard
  namespace: default
  labels:
    grafana_dashboard: "1"
data:
  genesis-dashboard.json: |-
{{ .Files.Get "../../monitoring/dashboards/genesis-dashboard.json" | indent 4 }}